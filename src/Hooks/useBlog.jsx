import React, { useEffect, useState } from 'react';

const useBlog = () => {
    const [wishList, setWishList] = useState([]);

    useEffect(()=> {
        
    },[])
    return [wishList]
};

export default useBlog;